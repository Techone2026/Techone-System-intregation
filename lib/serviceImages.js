// Optional real photos per service, keyed by slug. Static imports so
// Next.js can process them correctly under the GitHub Pages basePath.
// Add an entry here as real photos come in for other services.
import audioVisual from "@/public/images/services/audio-visual.jpg";

export const serviceImages = {
  "audio-visual": audioVisual,
};
